% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_regression.R
\name{net_regression_control}
\alias{net_regression_control}
\title{Control parameters for net_regression}
\usage{
net_regression_control(
  method = "qap",
  strategy = "sequential",
  family = "auto",
  estimator = "standard",
  mode = NULL,
  diag = NULL,
  seed = NULL,
  groups = NULL,
  ncores = NULL,
  use_robust_errors = FALSE,
  fixest_se_cluster = NULL,
  reference = NULL,
  comparison = NULL,
  random_intercept_nets = FALSE,
  random_intercept_sender = FALSE,
  random_intercept_receiver = FALSE,
  less_mem = FALSE,
  use_gpu = FALSE
)
}
\arguments{
\item{method}{\code{"qap"} (Dekker's double semi-partialling plus, default)
or \code{"qapy"} (permute y only).}

\item{strategy}{A \pkg{future} strategy string, e.g. \code{"sequential"}
(default) or \code{"multisession"}.}

\item{family}{Model family.  \code{"auto"} (default) resolves to
\code{"gaussian"} for weighted networks and \code{"binomial"} for binary
networks.  Other choices: \code{"gaussian"}, \code{"binomial"}, \code{"poisson"},
\code{"negbin"}, \code{"zip"}, \code{"multinom"}.}

\item{estimator}{\code{"standard"} (default, uses \code{lm}/\code{glm}) or \code{"gmm"}
(generalized method of moments; requires \pkg{gmm} and is available
for \code{"binomial"}, \code{"poisson"}, \code{"negbin"}, and \code{"zip"}).}

\item{mode}{\code{"directed"} or \code{"undirected"}.  Default \code{NULL} means
auto-detected from \code{.data}.}

\item{diag}{Logical; include diagonal (loop) ties in the estimation.
Default \code{NULL} means auto-detected.}

\item{seed}{Integer random seed.  Default \code{NULL}.}

\item{groups}{A vector (or list of vectors for multi-network fits)
assigning nodes to groups; permutations are performed within groups.
Default \code{NULL}.}

\item{ncores}{Number of parallel workers.  Default \code{NULL} (uses
the \code{strategy} setting).}

\item{use_robust_errors}{Logical; use HC3 heteroskedasticity-robust
standard errors.  Default \code{FALSE}.}

\item{fixest_se_cluster}{Column name to use as a cluster variable for
\pkg{fixest} standard errors.  Default \code{NULL}.}

\item{reference}{Reference category for \code{"multinom"} models.
Default \code{NULL}.}

\item{comparison}{Named list of pairwise comparisons for multinomial
models.  Default \code{NULL}.}

\item{random_intercept_nets}{Logical; include a random intercept for
network identity (requires \pkg{lme4}).  Default \code{FALSE}.}

\item{random_intercept_sender}{Logical; include a random intercept for
sender node (requires \pkg{lme4}).  Default \code{FALSE}.}

\item{random_intercept_receiver}{Logical; include a random intercept for
receiver node (requires \pkg{lme4}).  Default \code{FALSE}.}

\item{less_mem}{Logical; drop the baseline model object from the
returned fit to save memory.  Default \code{FALSE}.}

\item{use_gpu}{Logical; use torch-based batch OLS on the GPU
(gaussian family only; requires \pkg{torch}).  Default \code{FALSE}.}
}
\value{
A named list suitable for passing as the \code{control} argument of
\code{\link{net_regression}()}.
}
\description{
Returns a named list of control parameters for \code{\link{net_regression}()}.
This is the recommended way to specify advanced options because all
parameters are tab-completable and individually documented.

Set \code{options(infernet_advice = FALSE)} to suppress the specification
advice message that is printed when homophily terms are present.
}
\seealso{
\code{\link{net_regression}}

Other models:
\code{\link{regression}},
\code{\link{tests}}
}
\concept{models}
