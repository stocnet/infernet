% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qap_misc.R
\name{net_from_edgelist}
\alias{net_from_edgelist}
\title{Convert a long-format edge list to a named list of matrices}
\usage{
net_from_edgelist(
  df,
  sender,
  receiver,
  perceiver = NULL,
  mode = c("directed", "undirected"),
  loops = FALSE,
  multi_mode = FALSE,
  split_by = NULL
)
}
\arguments{
\item{df}{A data frame with one row per dyad.}

\item{sender}{Name of the column identifying the sender node.}

\item{receiver}{Name of the column identifying the receiver node.}

\item{perceiver}{Optional name of a third dimension (perceiver) for
Cognitive Social Structure (CSS) data.  Default \code{NULL}.}

\item{mode}{\code{"directed"} (default) or \code{"undirected"}.}

\item{loops}{Logical; include self-loops (diagonal).  Default \code{FALSE}.}

\item{multi_mode}{Logical; if \code{TRUE}, sender and receiver node sets
are treated as distinct (two-mode / bipartite).  Default \code{FALSE}.}

\item{split_by}{Optional column name; if supplied, one matrix per
unique value of this column is returned (useful for building a
list-of-networks input).  Default \code{NULL}.}
}
\value{
A named list.  Each element corresponds to a non-structural
column of \code{df} and is either a matrix (when \code{perceiver = NULL}) or a
3-D array.  When \code{split_by} is set the result is a list of such
named lists.
}
\description{
A convenience function for users who have dyadic data in long format
(one row per sender-receiver pair) and need to convert it into the
named list of matrices expected by \code{\link{net_regression}()}.
}
\examples{
df <- data.frame(
  from  = c("A", "A", "B", "B", "C", "C"),
  to    = c("B", "C", "A", "C", "A", "B"),
  weight = c(1, 2, 3, 4, 5, 6)
)
mats <- net_from_edgelist(df, sender = "from", receiver = "to")
mats$weight
}
\seealso{
\code{\link{net_regression}}

Other models:
\code{\link{regression}},
\code{\link{tests}}
}
\concept{models}
